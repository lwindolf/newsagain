{
  "type": "module",
  "scripts": {
    "test": "eslint src && NODE_OPTIONS='--experimental-vm-modules' jest --coverage"
  },
  "jest": {
    "verbose": true,
    "transform": {
      "^.+\\.js?$": "babel-jest"
    },
    "testEnvironment": "jest-environment-jsdom"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.20",
    "babel-jest": "^29.7.0",
    "eslint": "^8.49.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^28.1.3"
  }
}
